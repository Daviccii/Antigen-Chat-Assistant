[phases.build]
cmds = [
    "cd frontend && npm install",
    "cd backend && pip install -r requirements.txt"
]

[phases.start]
cmd = "cd backend && ./start.sh"

[variables]
PORT = "8000"
PYTHON_VERSION = "3.11"
NODE_VERSION = "18"